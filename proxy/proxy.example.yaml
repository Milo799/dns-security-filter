# DNS 代理中间件配置示例
# 复制为 config.yaml 并修改后使用

listen_addr: 0.0.0.0        # 监听地址
listen_port: 53              # 监听端口
upstream_addr: 10.0.0.50     # 安全过滤平台地址（必填）
upstream_port: 53            # 平台端口
forward_timeout: 3           # 转发超时（秒）
log_enabled: false           # 运行日志开关
